export enum Stage {
  DEVELOPMENT = 'development',
  PRODUCTION = 'prod',
}

export enum HttpSuccessCode {
  Created = 201,
}

export enum HttpClientErrorCode {
  BadRequest = 400,
  Unauthorized = 401,
  NotFound = 404,
  Conflict = 409,
}
